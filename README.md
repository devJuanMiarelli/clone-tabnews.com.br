# clone-tabnews.com.br
Treinando implementações no https://curso.dev/ com o Filipe.

- .nvmrc -> arquivo de versão do nvm (node), use 'nvm install'.

# diretórios
- pages -> diretório onde estarão todas as "páginas", que serão renderizadas pelo nextJS

# alguns comandos 'git'

- git log - listar os commits do repositório.
- git add - sobe alterações para a staging area.
- git commit - realiza novos commits.
- git commit --amend - substitui o commit anterior por um novo, mas aproveita as alterações dele.
- git diff - calcula a diferença entre as versões/alterações dos arquivos.
